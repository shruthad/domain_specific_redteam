pydantic>=2.0
PyYAML>=6.0
typer>=0.9
certifi>=2024.0.0
pytest>=7.0
# Optional, used when available for fuzzy deduplication.
rapidfuzz>=3.0
# Optional external red-team tools. Install manually if desired:
# deepteam
# garak
